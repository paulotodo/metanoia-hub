# a11y Checklist: a11y-ci-gate (Story 12.6 — Quality Gate Permanente de Acessibilidade no CI)

**Purpose**: Validar a QUALIDADE dos requisitos (nao da implementacao) da story 12.6 — gates permanentes de a11y no CI. "Unit tests for English" sobre spec.md + plan.md.
**Created**: 2026-06-17
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md)
**Gate type**: formal (release) · **Audience**: reviewer (PR)
**Areas de foco**: (1) gate axe consolidado; (2) check-contrast.ts coexistencia; (3) axe-final serious=0 + promocao warn->hard com pre-promocao FR-13; (4) workflow a11y-checks.yml + ci.yml; (5) relatorio de auditoria final do Epico 12.

## 1. Gate axe consolidado (axe-quality-gate.e2e-spec.ts + a11y-pages.json)

- [x] CHK001 - Sao os requisitos do gate axe bloqueante (`expect(violations).toHaveLength(0)`) especificados de forma nao-ambigua? [Clareza, Spec §FR-5/SC-2.8] {auto} — definido em SC-2.8 e plan §2.2 com assertion hard literal.
- [x] CHK002 - Esta o conjunto de paginas auditadas (escopo) definido e a fonte unica (`a11y-pages.json` na raiz) especificada? [Completude, Spec §FR-4/SC-2.7/SC-5.3] {auto} — paginas /, /login, /register, /recuperar-senha em plan §2.1; fonte unica raiz do repo.
- [x] CHK003 - Esta o criterio de filtragem `requiresAuth === false` (paginas publicas) especificado e o comportamento para `requiresAuth:true` (skip anotado) definido? [Clareza, Spec §NC-3/dec-007] {auto} — plan §2.2: `pages.filter(p => p.requiresAuth === false)` + `test.skip` anotado.
- [x] CHK004 - Estao as tags WCAG bloqueantes (`wcag2a/wcag2aa/wcag21aa`) e a exclusao de `best-practice` do conjunto bloqueante especificadas? [Clareza, Spec §plan 2.2] {auto} — plan §2.2 fixa as tags e exclui best-practice do gate.
- [x] CHK005 - E a iteracao dinamica (1 test por pagina, sem editar o spec ao adicionar pagina) um requisito explicito e mensuravel? [Mensurabilidade, Spec §FR-11/SC-5.3] {auto} — FR-11 + SC-5.3 + plan §2.2 (`for (const page of publicPages)`).
- [ ] CHK006 - Esta o caminho de resolucao do `a11y-pages.json` a partir do cwd do Playwright runner definido de forma nao-ambigua? [Ambiguity, Spec §plan 2.2] {auto} — plan §2.2 deixa "caminho a confirmar via process.cwd() do runner — execute-task valida"; resolucao adiada a execute-task. Aceitavel como decisao tecnica de implementacao, mas e um ponto de ambiguidade residual a confirmar.

## 2. check-contrast.ts (color2k) — coexistencia sem duplicacao

- [x] CHK007 - Esta a coexistencia entre `check-contrast.ts` (novo) e `check-contrast-tokens.mjs` (12.3) especificada com responsabilidades distintas e zero duplicacao? [Consistencia, Spec §NC-1/dec-005 + plan §3.1] {auto} — `.mjs` permanece no Lint; `.ts` cobre oklch relative-color + WCAG AA parametrizavel.
- [x] CHK008 - Estao os exit codes do `check-contrast.ts` (0 = pass, 1 = fail/hard) e o formato de saida especificados de forma testavel? [Mensurabilidade, Spec §SC-1.5/SC-1.6 + plan §3.2] {auto} — plan §3.2: sucesso exit 0 + mensagem; falha exit 1; SC-1.9 lista os casos de teste.
- [x] CHK009 - E a flag `--tokens-path` (com default e proibicao de hardcode) um requisito explicito? [Clareza, Spec §FR-1/SC-1.1 + plan §3.2] {auto} — `--tokens-path` default `packages/config/tailwind.preset.css`, "nunca hardcoded" (SC-1.1).
- [x] CHK010 - Estao os criterios de aceite dos testes unitarios Vitest (pass, fail+formato, --verbose, --tokens-path, sugestoes) definidos e mensuraveis? [Cobertura, Spec §FR-2/SC-1.9 + plan §3.3] {auto} — SC-1.9 enumera cobertura; plan §3.3 usa fixtures CSS sinteticos.
- [x] CHK011 - Esta a dependencia `color2k` (ausente) e a decisao de onde instalar (apps/web devDeps) especificada, junto de `@axe-core/playwright` (ja presente)? [Dependencias, Spec §FR-12 + plan §3.4] {auto} — plan §3.4: color2k ABSENT -> apps/web devDeps; @axe-core/playwright ja presente.

## 3. axe-final serious=0 + promocao warn->hard (motion-safe / i18n-scf) com pre-promocao FR-13

- [x] CHK012 - Esta o gap do `axe-final.spec.ts` (apenas critical=0, falta serious) identificado e a correcao (`expect(bySeverity.serious).toBe(0)`) especificada? [Completude, Spec §FR-9/NC-2/dec-006] {auto} — NC-2 com evidencia empirica (linha 114) + acao de execute-task definida.
- [x] CHK013 - Estao os requisitos de promocao do `check-motion-safe.sh` de `--warn` para hard (exit 1) especificados sem ambiguidade? [Clareza, Spec §FR-7/SC-3.1] {auto} — SC-3.1: `bash scripts/check-motion-safe.sh --ci` (sem --warn), exit 1.
- [x] CHK014 - Esta a adicao de `check-i18n-scf.sh --strict` ao job Lint do ci.yml como gate hard especificada? [Completude, Spec §FR-8/SC-3.2] {auto} — SC-3.2 + plan §5.2.
- [x] CHK015 - Esta o gate de PRE-PROMOCAO (FR-13/SC-3.4 — rodar os 3 scripts no repo e confirmar exit 0 ANTES de promover a hard) especificado como requisito obrigatorio, com a licao do PR #156 (CI vermelho permanente) registrada? [Risco/Completude, Spec §FR-13/SC-3.4 + plan §5.3] {auto} — plan §5.3 marca OBRIGATORIA no execute-task; cita "PR #156 — CI silenciosamente vermelho"; T12 cobre. RISCO CRITICO devidamente coberto pelos requisitos.
- [x] CHK016 - Estao os 3 scripts da pre-promocao enumerados explicitamente (motion-safe --ci, i18n-scf --strict, check-contrast.ts)? [Clareza, Spec §plan 5.3 + T12] {auto} — plan §5.3 lista os tres comandos; T12 da tabela de testes.
- [ ] CHK017 - Esta definido o comportamento esperado SE a pre-promocao falhar (algum script exit 1) — corrigir vs. registrar Decisao de bloqueio vs. nao-promover? [Clareza, Spec §plan 5.3] {auto} — plan §5.3 diz "NAO promover sem antes corrigir ... ou registrar Decisao de bloqueio". Coberto, mas o criterio de quando corrigir vs. bloquear fica a julgamento do execute-task (ambiguidade residual menor).
- [x] CHK018 - Esta o requisito de documentar (comentario de secao a11y no ci.yml) as mudancas de gate especificado? [Completude, Spec §SC-3.3 + plan §5.4] {auto} — SC-3.3 + plan §5.4 (modo, origem epico 12, cross-ref).

## 4. Workflow a11y-checks.yml (path-filtered) + ci.yml — YAML valido, sem duplicacao nociva

- [x] CHK019 - Esta o workflow dedicado `a11y-checks.yml` com path-filter especificado (paths exatos e ambos os steps hard, sem continue-on-error)? [Completude, Spec §FR-3/SC-2.1/SC-2.3] {auto} — SC-2.1 lista paths; SC-2.3 exige verde sem continue-on-error.
- [x] CHK020 - Estao os steps de setup do workflow (pnpm install, build web, start dev server, Chromium-only) especificados? [Completude, Spec §SC-2.5/SC-2.6] {auto} — SC-2.5 (setup) + SC-2.6 (Chromium-only, constraint CI).
- [x] CHK021 - Esta o upload de relatorio HTML axe como artifact do run especificado? [Completude, Spec §FR-6/SC-2.4] {auto} — FR-6 + SC-2.4.
- [x] CHK022 - Esta a NAO-duplicacao nociva entre a11y-checks.yml e o job E2E do ci.yml justificada e a divisao de responsabilidades (Lint=eixos B/D, E2E=eixos A/C, dedicado=espelha B+A) explicita? [Consistencia, Spec §plan 1.1] {auto} — plan §1.1 documenta divisao + nota de nao-duplicacao (fonte unica do spec/pages).
- [ ] CHK023 - E a VALIDADE do YAML do(s) workflow(s) um criterio de aceite explicito e verificavel (ex.: lint YAML / parse)? [Gap, Spec §DoD] {auto} — Apenas a execucao local via `act` (DoD §3) cobre validade indiretamente; nao ha SC dedicado exigindo "YAML valido / parseavel" como gate. Recomenda-se adicionar criterio explicito ou tarefa de validacao YAML em create-tasks.
- [x] CHK024 - Esta a integracao dos eixos B e D no job Lint e dos eixos A e C no job E2E do ci.yml especificada com pontos de insercao concretos? [Completude, Spec §plan 2.3/3.5/5] {auto} — plan §2.3 (E2E), §3.5 (Lint contrast), §5 (Lint promocoes) com linhas-alvo do ci.yml.

## 5. Relatorio de auditoria final do Epico 12

- [x] CHK025 - Esta o caminho e as secoes obrigatorias do relatorio (Escopo, Metodologia, Baseline, Final, Tech Debt R2, Gate Status) especificados? [Completude, Spec §FR-10/SC-4.1] {auto} — SC-4.1 enumera secoes; FR-10 fixa caminho `_bmad-output/implementation-artifacts/a11y/epic-12-audit-report.md`.
- [x] CHK026 - Estao as fontes de baseline e final (axe-baseline*.json / axe-final*.json / delta-report) especificadas para a comparacao por pagina e severidade? [Mensurabilidade, Spec §SC-4.2/SC-4.3 + plan §6] {auto} — plan §6 lista fontes e tabela por pagina/severidade.
- [x] CHK027 - Esta o conteudo "Gate Status" (axe-final verde, check-contrast verde, todos hard gates verdes) definido como confirmacao verificavel? [Mensurabilidade, Spec §SC-4.6] {auto} — SC-4.6 itens (a)(b)(c).
- [x] CHK028 - Esta o conflito de caminho do relatorio (spec FR-10 `_bmad-output/...` vs. briefing "docs/") reconciliado de forma explicita? [Conflict-resolved, Spec §plan 6] {auto} — plan §6 reconcilia: caminho autoritativo + stub/cross-link em docs/.
- [x] CHK029 - Estao os itens de tech debt R2 (paginas autenticadas, screen readers NFR-A4, text-muted decorativo etc.) listados com justificativa? [Completude, Spec §SC-4.5 + plan §6] {auto} — SC-4.5 + plan §6 (remanescentes deferidas).

## 6. Consistencia transversal, dependencias e premissas

- [ ] CHK030 - Existe conflito textual em SC-5.1 (exige "pelo menos uma pagina de fluxo autenticado-publico") vs. NC-3/dec-007 (que reajusta SC-5.1 para SO paginas publicas)? [Conflict, Spec §SC-5.1 vs §NC-3] {auto} — O texto original de SC-5.1 NAO foi editado e ainda pede pagina autenticada; NC-3 declara "SC-5.1 ajustado" mas a linha 121 permanece. Conflito textual a resolver via /clarify (semanticamente resolvido por dec-007, mas a spec contradiz a si mesma na letra).
- [x] CHK031 - Estao as premissas de estado do CI (motion-safe ja corrigido em 12.4, i18n em 12.5, paginas axe ja verdes pos-epico) documentadas como pre-condicao da promocao? [Assumption, Spec §SC-3.4 + plan §5] {auto} — SC-3.4 e plan §5/§5.3 assumem violacoes 12.4/12.5 corrigidas; FR-13 e a salvaguarda se a premissa falhar.
- [x] CHK032 - Esta a Definition of Done completa e cada item dela rastreavel a um FR/SC (testes verdes, act local, ci.yml atualizado, relatorio, todos os checks de PR, NC resolvidos)? [Completude, Spec §DoD] {auto} — DoD itens 1-7 mapeiam a FRs/SCs e NC-1/2/3.
- [ ] CHK033 - O apetite de risco da promocao simultanea de multiplos gates a hard nesta unica story (vs. promocao incremental) reflete a prioridade do produto? [Risco] {humano} — Decisao de produto/tech-lead: promover motion-safe + i18n-scf + adicionar 2 gates axe juntos concentra risco de CI vermelho; FR-13 mitiga, mas a cadencia e julgamento do dono.

## Notes

- Items `{auto}` ja vem resolvidos pelo agente (`[x]` com citacao, ou marcador de gap/ambiguity/conflict).
- Items `{humano}` ficam `[ ]` aguardando decisao do dono do produto.
- Rastreabilidade: 33/33 items com referencia (100%).

### Resolucao

- {auto} satisfeitos (`[x]`): 26
- {auto} abertos com marcador: 6 — CHK006 [Ambiguity], CHK017 [Ambiguity], CHK023 [Gap], CHK030 [Conflict] (+ CHK006/CHK017 sao residuais menores)
- {humano} aguardando decisao: 1 — CHK033

### Follow-up (gaps viram acao)

| Item | Marcador | Destino |
|------|----------|---------|
| CHK030 | [Conflict] | /clarify — reconciliar texto de SC-5.1 com NC-3/dec-007 (SC-5.1 ainda pede pagina autenticada na letra) |
| CHK023 | [Gap] | /create-tasks — tarefa: validar YAML dos workflows (parse/lint) como criterio explicito alem do `act` |
| CHK006 | [Ambiguity] | /create-tasks (execute-task) — confirmar cwd de resolucao do a11y-pages.json no runner Playwright |
| CHK017 | [Ambiguity] | /create-tasks (execute-task) — criterio corrigir-vs-bloquear se pre-promocao FR-13 falhar |
| CHK033 | {humano} | decisao do dono do produto: cadencia de promocao de gates (simultanea vs incremental) antes de execute-task |
